setup:
    composer:
        action: "install"
  

test:
    php_unit:
        config:
          - "phpunit.xml"
        run_from:  "."
        
    php_cpd:
        standard: "PSR2"
        path: "."
        ignore:
          - "vendor"
          - "Tests"
  
complete:
    clean_build:
        remove:
            - composer.json
            - composer.phar
            - config.dev.php        
        

