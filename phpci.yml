setup:
    composer:
        action: "install"
  

test:
    php_unit:
        config:
          - "phpunit.xml"
        run_from:  "."


complete:
    clean_build:
        remove:
            - composer.json
            - composer.phar
            - config.dev.php        
        

